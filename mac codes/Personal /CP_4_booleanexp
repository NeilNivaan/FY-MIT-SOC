#include <iostream>
using namespace std;
int main () {
    int age =  20;
    int marks = 75;
    int score = 85;

    bool isAdult = (age >= 18);
    bool isPassed = (marks >= 40);
    bool isTopper = (score >= 90);

    cout << " isAdult? : " << isAdult << endl ; 
    cout << " isPassed? : " << isPassed << endl ;
    cout << " isTopper? : " << isTopper << endl ;

    return 0;



}